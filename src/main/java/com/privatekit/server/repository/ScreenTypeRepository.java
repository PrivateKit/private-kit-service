package com.privatekit.server.repository;

import com.privatekit.server.entity.ScreenType;
import org.springframework.data.repository.CrudRepository;

public interface ScreenTypeRepository extends CrudRepository<ScreenType,String> {
}
